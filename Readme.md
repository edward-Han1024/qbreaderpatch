# qbreaderpatch

A python script that generates a qbreader .json set that can be used on the qbreader website. It can be ran by invoking `python3 main.py`, which will download and process the data.

It is recommended to set the reading speed to be the lowest on qbreader and enable randomize order. General Science and Energy have no subcategory. ESS is put into Astronomy.
